# CI Status

Shows recent GitHub Actions workflow runs for the current branch (build main, doc tests, etc.).

## Usage

```bash
bash .cursor/commands/gbx-ci-status.sh [LIMIT]
```

## Options

- **LIMIT** (optional) — Number of recent runs to show (default: 10).

## Prerequisites

- GitHub CLI (`gh`) installed and authenticated.
