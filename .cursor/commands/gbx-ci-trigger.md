# Trigger CI Workflow (manual)

Pushes the current branch to origin, then prompts to trigger the **build main** workflow on that branch.

## Usage

```bash
bash .cursor/commands/gbx-ci-trigger.sh
```

## Prerequisites

- GitHub CLI (`gh`) installed and authenticated.
