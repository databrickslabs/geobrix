---
sidebar_position: 3
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeFromTest from '@site/src/components/CodeFromTest';
import quickstartCode from '!!raw-loader!../tests/python/quickstart/examples.py';

# Quick Start

This guide will help you get started with GeoBrix quickly after installation. All code below is copy-paste executable when you use the sample data paths shown.

## Choose your execution tier

GeoBrix ships in two tiers that share the same function names — pick one with the toggle below and the rest of this page follows your choice (all tabs grouped under the same tier toggle stay in sync).

<Tabs groupId="gbx-tier" queryString="tier" className="gbx-tier-tabs">
<TabItem value="lightweight" label="Lightweight — recommended">

Pure-Python (rasterio / pyogrio / NumPy), no JAR and no GDAL or OGR to install. A single wheel via `%pip` or as a cluster library, and it runs on serverless, standard/shared, ARM, and Lakeflow declarative pipelines. Covers **RasterX** (every `rst_*` function), **VectorX** (`gbx_st_*`), and the **GridX quadbin** grid (`gbx_quadbin_*`), plus the lightweight readers.

```python
# Stage the wheel in a Unity Catalog Volume, then install the [light_env6] extra.
# Use the quoted PEP 508 "name[extra] @ file://" form (Serverless-safe):
%pip install "geobrix[light_env6] @ file:///Volumes/<catalog>/<schema>/<volume>/geobrix-<version>-py3-none-any.whl"

from databricks.labs.gbx.pyrx import functions as rx
rx.register(spark)   # installs the gbx_rst_* SQL names, pyspark-backed
```

:::warning Quote the `geobrix[light_env6] @ file://…` requirement
Install with the **named, quoted** form above. Do **not** put the extra on the
path (`'/Volumes/…/…whl[light_env6]'`): on **Serverless**, `%pip` writes the
requirement to a file *including the quotes*, so pip reads `[light_env6]` as part of
the filename and fails with *"Expected package name at the start of dependency
specifier."* The named form installs cleanly on Serverless, standard/shared,
and ARM.
:::

</TabItem>
<TabItem value="heavyweight" label="Heavyweight">

JVM-native (Scala + GDAL/OGR), installed via the JAR + init script. Covers everything the lightweight tier does **plus** the heavy-only deltas: the GDAL/OGR readers (`gtiff_gdal`, `gdal`, `shapefile_ogr`, `geojson_ogr`, `gpkg_ogr`, `file_gdb_ogr`). Choose this tier when your environment is already JVM/GDAL-based or when you need one of those deltas.

```python
from databricks.labs.gbx.rasterx import functions as rx
rx.register(spark)
```

</TabItem>
</Tabs>

The two tiers expose the same module alias (`rx` / `vx` / `gx`) and the same `gbx_*` SQL names, so most code on this page is identical across tiers — only the import/register line and the reader format strings differ. The tabs below sync to your choice; where a block has no tabs, the code is identical on both tiers.

See [Installation](./installation) for the two install paths and [Choosing an Execution Tier](./api/execution-tiers) for the full comparison.

:::tip Set up sample data first
The examples on this page use **sample data paths** (e.g. `/Volumes/.../geobrix-examples/nyc/...`). To run them as-is, set up sample data first: **[Sample Data Guide](./sample-data/overview)** — then use the same paths or volume in your notebook.
:::

## Prerequisites

- [Install GeoBrix](./installation) on your Databricks cluster.
- (Recommended) [Set up sample data](./sample-data/overview) so the snippets below run without changing paths.

:::note
You do not need to register functions if you are only using the included readers.
:::

## Register Functions

The import/register line is the one place each package genuinely differs by tier. Pick your tier; the function names you call afterward are the same either way.

<Tabs groupId="gbx-tier" queryString="tier" className="gbx-tier-tabs">
<TabItem value="lightweight" label="Lightweight">

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="REGISTER_RASTERX_LIGHT"
  testFile="docs/tests/python/quickstart/test_examples.py"
/>

Register VectorX and the GridX quadbin grid the same way:

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="REGISTER_VECTORX_LIGHT"
  testFile="docs/tests/python/quickstart/test_examples.py"
/>

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="REGISTER_QUADBIN_LIGHT"
  testFile="docs/tests/python/quickstart/test_examples.py"
/>

</TabItem>
<TabItem value="heavyweight" label="Heavyweight">

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="REGISTER_RASTERX"
  testFile="docs/tests/python/quickstart/test_examples.py"
/>

The same in Scala:

<CodeFromTest
  code={quickstartCode}
  language="scala"
  functionName="REGISTER_RASTERX_SCALA"
  title="Register RasterX (Scala)"
  source="docs/tests/python/quickstart/examples.py"
  testFile="docs/tests/python/quickstart/test_examples.py"
/>

Register GridX (BNG) and VectorX the same way:

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="REGISTER_GRIDX"
  testFile="docs/tests/python/quickstart/test_examples.py"
/>

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="REGISTER_VECTORX"
  testFile="docs/tests/python/quickstart/test_examples.py"
/>

</TabItem>
</Tabs>

## List and Describe Functions

<CodeFromTest
  code={quickstartCode}
  language="sql"
  functionName="SQL_LIST_FUNCTIONS"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="SQL_LIST_FUNCTIONS_output"
/>

<CodeFromTest
  code={quickstartCode}
  language="sql"
  functionName="SQL_DESCRIBE"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="SQL_DESCRIBE_output"
/>

## Reading Data

Paths below assume the [Sample Data](./sample-data/overview) layout (e.g. Essential Bundle). The reader format string is the part that differs by tier: lightweight uses the pure-Python `*_gbx` readers (no GDAL/OGR install); heavyweight uses the GDAL/OGR readers.

<Tabs groupId="gbx-tier" queryString="tier" className="gbx-tier-tabs">
<TabItem value="lightweight" label="Lightweight">

Register the lightweight readers once per session, then load by format string (`gtiff_gbx` / `raster_gbx` for rasters; `shapefile_gbx` / `geojson_gbx` / `gpkg_gbx` / `file_gdb_gbx` for vectors).

#### Read GeoTIFF

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="READ_GEOTIFF_LIGHT"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="READ_GEOTIFF_LIGHT_output"
/>

:::note Virtual tiles by default
The lightweight raster reader returns **[virtual tiles](./api/virtual-tiles)** by default — each row
carries a bytes-free `(path, window)` reference instead of the raster pixels, so a large file fans
into tiles without accumulating memory. Pass `.option("virtualTiles", "false")` (or use
`materialize=True` on a downstream `rst_*` call) when you need the bytes in-row.
:::

#### Read Shapefile

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="READ_SHAPEFILE_LIGHT"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="READ_SHAPEFILE_LIGHT_output"
/>

</TabItem>
<TabItem value="heavyweight" label="Heavyweight">

The heavyweight tier reads through the GDAL/OGR readers — `gdal` / `gtiff_gdal` for rasters; `shapefile_ogr` / `geojson_ogr` / `gpkg_ogr` / `file_gdb_ogr` for vectors.

#### Read GeoTIFF

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="READ_GEOTIFF"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="READ_GEOTIFF_output"
/>

#### Read Shapefile

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="READ_SHAPEFILE"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="READ_SHAPEFILE_output"
/>

#### Read GeoJSON

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="READ_GEOJSON"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="READ_GEOJSON_output"
/>

</TabItem>
</Tabs>

## Using RasterX, GridX, and VectorX

### RasterX

Identical on both tiers — only the import/register differs (see the tier toggle above). Once registered, the `rx.rst_*` calls are the same.

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="USE_RASTERX"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="USE_RASTERX_output"
/>

### GridX

All GridX grids — **quadbin** (`gx.quadbin_*` / `gbx_quadbin_*`), **BNG** (`gbx_bng_*`), and **custom grids** (`gbx_custom_*`) — are available on **both** tiers.

<Tabs groupId="gbx-tier" queryString="tier" className="gbx-tier-tabs">
<TabItem value="lightweight" label="Lightweight">

Index a point to a quadbin cell and read its resolution:

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="USE_QUADBIN"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="USE_QUADBIN_output"
/>

</TabItem>
<TabItem value="heavyweight" label="Heavyweight">

The heavyweight tier adds the BNG grid (`gbx_bng_*`) and custom grids on top of quadbin:

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="USE_BNG"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="USE_BNG_output"
/>

</TabItem>
</Tabs>

### VectorX

Identical on both tiers — only the import/register differs (see the tier toggle above). The `vx.st_*` calls and `gbx_st_*` SQL names are the same.

<CodeFromTest
  code={quickstartCode}
  language="python"
  functionName="USE_VECTORX"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="USE_VECTORX_output"
/>

## Using SQL

The `gbx_*` SQL names are identical on both tiers (only the reader format string differs — see [Reading Data](#reading-data)). This example uses a heavyweight reader; swap `shapefile_ogr` for `shapefile_gbx` on the lightweight tier.

<CodeFromTest
  code={quickstartCode}
  language="sql"
  functionName="SQL_READ_AND_USE"
  testFile="docs/tests/python/quickstart/test_examples.py"
  outputConstant="SQL_READ_AND_USE_output"
/>

## Next Steps

- [Sample Data](./sample-data/overview) — Download and use sample data in examples
- [Readers](./readers/overview) — Shapefile, GeoJSON, GDAL, and more
- [API Reference](./api/overview) — Function reference
- [Raster Functions](./api/raster-functions) · [GridX](./api/gridx-functions) · [VectorX](./api/vectorx-functions)
