"""Tier-neutral GeoBrix core — pure-Python building blocks shared across the
lightweight packages (pyrx, pyvx, pygx) with no tier-specific (raster/vector)
dependencies. Currently: the CRS resolver (``core.crs``)."""
